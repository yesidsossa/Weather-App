import Foundation

enum LocalizedKeys {
    enum Search {
        static let placeholder = "search_placeholder"
        static let errorTitle = "search_error"
        static let noResults = "search_no_results"
    }

    enum WeatherDetails {
        static let temperature = "weather_temperature"
        static let title = "weather_detail_title"
    }

    enum Favorites {
        static let title = "weather_favorites_title"
        static let remove = "weather_remove_favorite"
    }
}
